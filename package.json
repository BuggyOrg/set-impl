{
  "name": "@buggyorg/set-impl",
  "version": "0.2.8",
  "description": "A tool that replaces set operations with concrete implementations.",
  "main": "lib/api.js",
  "scripts": {
    "test": "mocha --recursive --compilers js:babel-register && standard src/**/*.js",
    "build": "rm -rf lib && babel src -d lib",
    "prepublish": "rm -rf lib && babel src -d lib"
  },
  "keywords": [
    "Buggy"
  ],
  "author": "Maik Marschner",
  "license": "MIT",
  "dependencies": {
    "@buggyorg/graphtools": "^0.4.0-pre.39",
    "@buggyorg/lisgy": "^0.2.0-pre.15",
    "@buggyorg/rewrite": "^0.2.11",
    "cli-ext": "^1.1.0",
    "lodash.memoize": "^4.1.2"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.4.3",
    "chai": "^3.4.1",
    "jsdoc": "^3.4.0",
    "mocha": "^3.2.0",
    "standard": "^9.0.0"
  }
}
